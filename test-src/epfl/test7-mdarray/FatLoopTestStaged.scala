package scala.virtualization.lms
package epfl
package test7

import original.MDArray

import common._
import test1.Arith

class FatLoopTestStaged() { this: MDArrayBaseExp with IfThenElseExp =>

//  def testStaged(a: Rep[MDArray[Int]], b: Rep[MDArray[Int]]): Rep[MDArray[Int]] =
//    With(function = (iv => sel(iv, a + b))).GenArray(shape(a))
}
